import math 
def oneguess(minimum,maximum):
    minimum = raw_input ("what is the minimum number?:")
    maximum = raw_input ("what is the maximum number?:")
    target = random.randint .format(int(minimum) .format(int(maximum))
    guess = raw_input ("guess a number between" .format(int(minimum) "and" .format(int(maximum))
if guess == target guess = True
if guess > target = False 
if guess < target = False


 
