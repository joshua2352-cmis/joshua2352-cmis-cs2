import math 
def findx((x * y ) / b = a):
    no1 = raw_input("this will find x any comments:")
    x = raw_input("enter first numero this will be the number being divided i.e y:")
    y = raw_input("enter second number this will be the number dividing i.e b:")
    a = raw_input("enter third number this will be what x * y /b will equal enter any number")
    return (x * y / b) = a 
      
    
